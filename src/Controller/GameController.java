package Controller;

import View.MainFrame;
import Model.FighterStatus;
import Model.ActionPanel;

public class GameController {
	
	
/**
	
	public String analyzeHp(){
		if (isDead = true) {
			
		}
		angriff enum = head
		defense enum = torso
		if angriff enum == defense enum{
			verteidiger.setHP(verteidiger.gethp() - hit)
		}
		
		return null;
	}
	
	public void handleActionButton(){
		if (true) {
			Swap Buttons from Attacker to Defender
		}
			wait for Defender Input
			actionPerformed(Enum) -> actionButton()
		};
	}
	
	public static void main(String[] args) {
		MainFrame gui = new MainFrame();
		gui.setSize(1920, 1080);
		gui.setVisible(true);
		gui.setResizable(false);
		
		!START		actionPerformed(enum)? -> actionButton()
		
		handleActionButton(turnOne Ergebnis);
		
		analyzeHP
	}
	
*/

}
